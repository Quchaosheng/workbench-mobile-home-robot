# Multi-scenario phase gates

<!-- Generated from docs/releases/phase-status-v1.json. Do not edit by hand. -->

This page is generated by `tools/scripts/phase_gates.py`. It states which
multi-scenario delivery phase proves itself today, and what evidence class each
phase reaches. It grants no release approval and is not physical or Gazebo
evidence.

- Code revision: `19f6943e035162508eb4320406c4920081d02c58`
- Configuration hash: `b5cf50457d285f026b0fbc498b69622b81ab44192c3fc47232ec89dbd9ea488f`
- Phases: 4
- Complete: 3
- Incomplete: 0
- Failed: 0
- Blocked: 1
- Release eligible: 0
- Sign-offs outstanding: 9
- Readiness report: available at `docs/evaluation/readiness-report-v1.json`

## Phases

| Phase | Name | Issues | Gate status | Completed | Evidence class | Release eligible |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | contract and registry | #308, #300 | COMPLETE | yes | scripted_fixture | no |
| 2 | task migration, evidence identity and recovery | #301, #302, #305 | COMPLETE | yes | scripted_fixture | no |
| 3 | first registry-native scenario | #306 | BLOCKED | no | none | no |
| 4 | dashboard, quality gates and readiness report | #303, #304, #307 | COMPLETE | yes | scripted_fixture | no |

## Gate detail

### Phase 1: contract and registry

Status: **COMPLETE** (completed: yes, evidence class: `scripted_fixture`, release eligible: no)

| Probe | Reference | Gate status | Detail |
| --- | --- | --- | --- |
| `scenario_contract` | `tools/scripts/check_scenario_contract.py` | PASS | scenario contract scenario-registry-v1 is frozen and loadable |
| `scenario_registry` | `tools/scripts/check_scenario_contract.py` | PASS | the registry loaded 5 fail-closed manifest(s) |

Entry criteria:

- the four-phase split is agreed and the parent epic (#309) is open
- no scenario manifest is authoritative until the registry contract is frozen

Exit criteria:

- the registry contract is frozen as a reviewed decision record (#308)
- the registry loads every committed manifest fail-closed (#300)
- a scenario registered without a row in the conformance corpus is refused

Required evidence:

- contract-valid manifests
- a fail-closed registry load
- generated conformance cases

Sign-off outstanding:

- Architecture owner: outstanding - the registry contract is a reviewed ADR
- Release owner: outstanding - the phase is recorded in the release notes

Rollback: revert this phase's commit; no later phase depends on a fixture that changes

### Phase 2: task migration, evidence identity and recovery

Status: **COMPLETE** (completed: yes, evidence class: `scripted_fixture`, release eligible: no)

| Probe | Reference | Gate status | Detail |
| --- | --- | --- | --- |
| `scenario_migration` | `tools/scripts/check_scenario_migration.py` | PASS | 5 legacy task famil(ies) resolve to a registered identity |
| `run_identity` | `tools/scripts/check_run_identity.py` | PASS | the run identity binds scenario, evidence and stream, and refuses a missing version |
| `recovery_policy` | `services/agent_runtime/workbench_agent_runtime/recovery.py` | PASS | a declared policy is bounded, an unknown action and a terminal stop are refused |

Entry criteria:

- phase 1 is software complete
- the shared safety, provenance, replay and evidence boundaries are written down

Exit criteria:

- every legacy task family resolves to a registered identity with a pinned corpus (#301)
- a stored run resolves to the scenario definition it was produced from (#302)
- recovery is bounded, evidence-aware and shared across scenarios (#305)

Required evidence:

- a pinned migration digest
- run identity bindings
- bounded recovery decisions

Sign-off outstanding:

- World Model owner: outstanding - run identity and evidence policy
- Agent Runtime owner: outstanding - the bounded recovery policy
- Release owner: outstanding - the phase is recorded in the release notes

Rollback: revert this phase's commit; the legacy entry points keep working until v0.4

### Phase 3: first registry-native scenario

Status: **BLOCKED** (completed: no, evidence class: `none`, release eligible: no)

| Probe | Reference | Gate status | Detail |
| --- | --- | --- | --- |
| `registry_native_scenario` | `sim/registry/*.json` | NOT_DELIVERED | no registry-native scenario is registered yet (issue #306 is not delivered) |
| `scenario_conformance` | `tools/scripts/check_scenario_conformance.py` | PASS | 5 registered scenario(s) prove all 7 dimension(s) |

Entry criteria:

- phase 2 is software complete
- a new scenario can be added without a legacy task_id or a second implementation

Exit criteria:

- cleaning-and-inspection is registered through the shared contract, not a side path (#306)
- the scenario passes the same conformance corpus as every migrated family

Required evidence:

- a registry-native manifest
- a conformance case set
- a reconciled capability matrix row

Blocked on:

- #306 is not delivered: promoting clean_workspace into the shared semantic ActionType touches libs/contracts and interfaces/json_schema, which AGENTS.md gates behind three human approvals
- the conformance corpus has no case set for a registry-native identity yet

Sign-off outstanding:

- Scenario owner: outstanding - the new manifest and its verifier
- Release owner: outstanding - the hold is recorded in the release notes

Rollback: hold the phase; the registry keeps the migrated families and the declared scenario stays unregistered

### Phase 4: dashboard, quality gates and readiness report

Status: **COMPLETE** (completed: yes, evidence class: `scripted_fixture`, release eligible: no)

| Probe | Reference | Gate status | Detail |
| --- | --- | --- | --- |
| `scenario_conformance` | `tools/scripts/check_scenario_conformance.py` | PASS | 5 registered scenario(s) prove all 7 dimension(s) |
| `readiness_report` | `tools/scripts/check_readiness_report.py` | PASS | the readiness report covers 5 identity(ies) |

Entry criteria:

- phases 1 to 3 are software complete
- every registered scenario has a scenario identity the surfaces can read

Exit criteria:

- a run and its evidence timeline can be inspected without hand-reading a bundle (#303)
- every registered scenario is gated on the shared boundaries (#304)
- one generated readiness report states what the software is ready for (#307)

Required evidence:

- a bounded run projection
- a conformance corpus over every registered identity
- a reproducible readiness artifact

Sign-off outstanding:

- Release / QA owner: outstanding - the readiness report and the phase table
- Project Owner: outstanding - the release decision the notes describe

Rollback: revert this phase's commit; the readiness page and the dashboard are additive surfaces

## Known limitations

- every reachable evidence class is software or scripted_fixture: no Gazebo or physical run backs any phase
- a phase is complete only in the sense that its declared gates pass; it is not release eligible
- each phase records the owner sign-off it still needs, and no gate here can observe a human decision
- this status grants no release approval and replaces no required check or human approval

## Verifying locally

```bash
python3 tools/scripts/phase_gates.py --output docs/releases/phase-status-v1.json
python3 tools/scripts/check_phase_gates.py
python3 -m pytest tests/unit/test_phase_gates.py -v
```
